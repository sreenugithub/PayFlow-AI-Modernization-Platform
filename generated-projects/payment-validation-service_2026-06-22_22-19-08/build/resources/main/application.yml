server:
  port: 9000

spring:
  application:
    name: payment-validation-service

external:
  account-validation:
    base-url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info
